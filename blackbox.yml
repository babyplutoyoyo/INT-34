modules:
  http_2xx:
    prober: http
    timeout: 5s
    http:
      method: GET
      fail_if_ssl: false  # Проверка SSL не обязательна
      valid_http_versions: ["HTTP/1.1", "HTTP/2.0"]
      valid_status_codes: []  # 200 - 299
      preferred_ip_protocol: "ip4"  # Использовать IPv4
